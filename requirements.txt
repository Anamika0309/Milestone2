# =========================================================================
# Groww Mutual Fund RAG Chatbot — Streamlit Cloud Requirements
# Only includes packages needed for the SERVING path (chatbot UI).
# For the full ingestion pipeline (torch, sentence-transformers, playwright),
# see requirements_full.txt
# =========================================================================

# Streamlit UI
streamlit>=1.32.0

# HTTP Client (Groq API calls)
httpx==0.27.0

# YAML Configuration
PyYAML==6.0.1

# Data Processing
numpy>=1.24.0
pandas>=2.1.0

# BM25 Sparse Search (lightweight, pure Python)
rank-bm25==0.2.2

# Logging
structlog==23.2.0

# Environment variable management
python-dotenv>=1.0.0
